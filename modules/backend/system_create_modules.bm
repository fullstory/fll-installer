##
##-------------------------------------
# Needs:
# TARGET_MNT_POINT
#
# Calls:
# logit
#
#-----------------------------------
# Everything needed for hw-detection
##-------------------------------------
##
function system_create_modules()
{
	#
	# log my call
	#
	logit $"system_create_modules"
	#

	if grep -q ^capability /proc/modules; then
		echo capability >> "$TARGET_MNT_POINT/etc/modules"
	fi
}
